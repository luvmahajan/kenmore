<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Current_YTD_Revenue__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(MONTH(TODAY())=1,YEAR( Invoice_Date__c ) = YEAR(TODAY())), Total_Revenue__c , IF(AND(YEAR( Invoice_Date__c  ) = YEAR(TODAY()),MONTH(Invoice_Date__c )&gt; 1), Total_Revenue__c ,0))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Current YTD Revenue</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
