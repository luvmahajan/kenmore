<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Current_User_on_Account_Team__c</fullName>
    <externalId>false</externalId>
    <formula>IF( OwnerId  = $User.Id, True,
 IF(NAM__c= $User.Id, True,
 IF(BDM__c = $User.Id, True,
 IF(CSS__c = $User.Id, True,False))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Current User on Account Team?</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
